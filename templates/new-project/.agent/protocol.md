# Core Protocol Placeholder

Use the protocol repository's core protocol files as the authoritative source.

In a real project, this file should point to the adopted collaboration rules or contain the vendored core protocol.

